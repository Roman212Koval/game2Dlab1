/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class PlayerStay : MonoBehaviour
{
    private enum State
    {
        STATE_ONFLOOR,
        STATE_JUMPING,
        STATE_OFF_ROAD,
        STATE_DIED
    };

    private State _curentState;
    // Start is called before the first frame update
    void Start()
    {
        _curentState = State.STATE_ONFLOOR;
    }

    public void onRoad()
    {
        switch (_curentState)
        {
            //case State.STATE_OFF_ROAD:

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/